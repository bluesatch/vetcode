// functions yay!!!!

function addFive(num) {
    var result = num + 5;
    console.log(result);
}

// addFive(10);

function greeting() {
    var hello = "hello world";
    console.log(hello);
}

// greeting();

function triangleArea(base, height) {
    var area = 1/2 * base * height;
    console.log(area);
}

// triangleArea(3, 4); // expects 6

// triangleArea(5, );

// var boo = 2;
// var foo = '2';

// var baz = boo / foo;
// console.log(baz);

// function boo(num) { // var num = 9;
//     var result = num * 12; // 9 * 12;
//     return result;
// }

// boo(9); //expect 108

// console.log(boo(9));

// function baz(foo) { // var foo = 9;
//     var newResult = boo(foo) / 2; // boo(9) -> 108 / 2;
//     console.log(newResult); // 54
// }

// baz(9);

// function fahToCels(fah) {
//     var celsius = (fah - 32) * 5 / 9;
//     console.log(celsius);
// }

// fahToCels(212);
// fahToCels(89);
// fahToCels(32)

// Write a function that will calculate the amount of PTO you receice in a year

// function ptoCalc(hours, checksPerYear) {
//     var ptoPerYear = hours * checksPerYear;
//     console.log(ptoPerYear); 
// }

// ptoCalc(4, 24);